import { AdaptiveWorkflowExecution } from '@ai/workflows/AdaptiveExecution';

describe('AdaptiveWorkflowExecution', () => {
  it('should be defined', () => {
    expect(AdaptiveWorkflowExecution).toBeDefined();
  });
});