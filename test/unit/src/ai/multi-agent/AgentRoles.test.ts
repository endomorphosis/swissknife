import { AgentRoleManager } from '../../../../src/ai/multi-agent/AgentRoles';

describe('AgentRoleManager', () => {
  it('should be defined', () => {
    expect(AgentRoleManager).toBeDefined();
  });
});