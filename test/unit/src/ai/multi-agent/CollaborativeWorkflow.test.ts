import { CollaborativeWorkflow } from '../../../../src/ai/multi-agent/CollaborativeWorkflow';

describe('CollaborativeWorkflow', () => {
  it('should be defined', () => {
    expect(CollaborativeWorkflow).toBeDefined();
  });
});