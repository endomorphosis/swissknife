import { CloudIntegrations } from '../../../../src/integrations/cloud/CloudIntegrations';

describe('CloudIntegrations', () => {
  it('should be defined', () => {
    expect(CloudIntegrations).toBeDefined();
  });
});