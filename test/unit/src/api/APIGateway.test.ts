import { SwissKnifeAPIGateway } from '../../../../src/api/APIGateway';

describe('SwissKnifeAPIGateway', () => {
  it('should be defined', () => {
    expect(SwissKnifeAPIGateway).toBeDefined();
  });
});