import { HuggingFaceBackend } from '@storage/vfs/backends/HuggingFaceBackend';

describe('HuggingFaceBackend', () => {
  it('should be defined', () => {
    expect(HuggingFaceBackend).toBeDefined();
  });
});