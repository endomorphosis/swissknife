import { StorachaBackend } from '../../../../src/storage/vfs/backends/StorachaBackend.js';

describe('StorachaBackend', () => {
  it('should be defined', () => {
    expect(StorachaBackend).toBeDefined();
  });
});