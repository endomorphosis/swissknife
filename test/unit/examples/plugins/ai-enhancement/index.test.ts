import { AIEnhancementPlugin } from '@examples/plugins/ai-enhancement/index';

describe('AIEnhancementPlugin', () => {
  it('should be defined', () => {
    expect(AIEnhancementPlugin).toBeDefined();
  });
});