import { ExternalIntegrationsPlugin } from '../../../../examples/plugins/external-integrations/index';

describe('ExternalIntegrationsPlugin', () => {
  it('should be defined', () => {
    expect(ExternalIntegrationsPlugin).toBeDefined();
  });
});