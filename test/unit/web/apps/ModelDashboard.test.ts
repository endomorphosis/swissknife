import { ModelDashboardApp } from '../../../../web/src/apps/ModelDashboard';

describe('ModelDashboardApp', () => {
  it('should be defined', () => {
    expect(ModelDashboardApp).toBeDefined();
  });
});