# Browser Deployment Policy

Generated by `node scripts/audit-browser-deployment-policy.mjs --report docs/browser-deployment-policy.md`.

## Scope

- Content-Security-Policy for the served web entry (`web/index.html`) and the deployment `_headers` file.
- Worker creation: only `src/workers/browser.ts` (Web Worker APIs) may reach a browser deployment bundle; `src/workers/host.ts` (`worker_threads`/`child_process`) is host-only.
- Storage APIs: only `src/storage/browser.ts` (IndexedDB, OPFS, Cache Storage, injected IPFS) may reach a browser deployment bundle; `src/storage/host.ts` (Node `fs`/`path`/`os`) is host-only.
- WASM isolation: cross-origin isolation headers (`Cross-Origin-Opener-Policy`, `Cross-Origin-Embedder-Policy`) required for `SharedArrayBuffer` and multi-threaded WASM.
- Offline behavior: service worker, web app manifest, and offline fallback page required for the browser deployment to keep working without a network connection.
- Built bundle host-leakage: when `dist/.vite/swissknife-bundle-metadata.json` exists, verifies no chunk pulls in `src/workers/host.ts` or `src/storage/host.ts`.

## Summary

- Package: `swissknife@0.0.53`
- CSP present: yes
- CSP missing required directives: 0
- Manifest linked from web entry: yes
- Service worker registered from web entry: yes
- Browser worker entrypoint host-only imports: 0
- Browser storage entrypoint host-only imports: 0
- Storage uses IndexedDB / OPFS / Cache Storage: yes / yes / yes
- Cross-origin isolation headers (dev/preview): 2/2
- Cross-origin isolation headers (deployment `_headers`): 2/2
- Offline artifacts present: 3/3
- Bundle metadata present: yes
- Host-only modules found in built bundle: 0
- Findings: 0 (0 violation(s))

## Content-Security-Policy

Entry: `web/index.html`

```
default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://esm.sh https://cdn.skypack.dev; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com; img-src 'self' data: https: blob:; font-src 'self' data: https://fonts.gstatic.com https://cdnjs.cloudflare.com; connect-src 'self' http://localhost:* http://127.0.0.1:* https:; worker-src 'self' blob:; child-src 'self' blob:; media-src 'self' blob: data:; object-src 'none'; base-uri 'self'; manifest-src 'self'; frame-ancestors 'none'
```

| Directive | Required | Present | Value |
| --- | --- | --- | --- |
| `base-uri` | yes | yes | `'self'` |
| `child-src` | no | yes | `'self' blob:` |
| `connect-src` | yes | yes | `'self' http://localhost:* http://127.0.0.1:* https:` |
| `default-src` | yes | yes | `'self'` |
| `font-src` | yes | yes | `'self' data: https://fonts.gstatic.com https://cdnjs.cloudflare.com` |
| `frame-ancestors` | recommended | yes | `'none'` |
| `img-src` | yes | yes | `'self' data: https: blob:` |
| `manifest-src` | recommended | yes | `'self'` |
| `media-src` | no | yes | `'self' blob: data:` |
| `object-src` | yes | yes | `'none'` |
| `script-src` | yes | yes | `'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://esm.sh https://cdn.skypack.dev` |
| `style-src` | yes | yes | `'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com` |
| `worker-src` | yes | yes | `'self' blob:` |

Directives such as `frame-ancestors` are ignored by browsers when set via `<meta http-equiv>` and must be delivered as a real HTTP response header; see `web/public/_headers` and the WASM Isolation section below.

## Worker Deployment Policy

- Browser worker entrypoint: `src/workers/browser.ts` (exists: yes)
- Host worker entrypoint: `src/workers/host.ts` (exists: yes, host-only, never bundled for the browser)
- Browser entrypoint host-only imports found: 0
- `build-tools/configs/vite.workers.config.ts` externalizes all host-only worker modules: yes
- `build-tools/configs/vite.web.config.ts` denies host-only worker module imports at resolve time: yes
- `build-tools/configs/vite.web.config.ts` pins `worker.format` to `es`: yes

Browser code must reach worker execution only through `src/workers/browser.ts` (`Worker`/`SharedWorker` construction, `postMessage`/transferables). Node's `worker_threads`, subprocess, and filesystem-backed worker resolution in `src/workers/host.ts` is host-only.

## Storage Deployment Policy

- Browser storage entrypoint: `src/storage/browser.ts` (exists: yes)
- Host storage entrypoint: `src/storage/host.ts` (exists: yes, host-only, never bundled for the browser)
- Browser entrypoint host-only imports found: 0
- Uses IndexedDB: yes
- Uses OPFS (`navigator.storage.getDirectory()`): yes
- Uses Cache Storage API: yes

Browser code must reach persistent storage only through `src/storage/browser.ts` (IndexedDB, OPFS, Cache Storage, or an explicitly injected IPFS transport). Node's filesystem, `path`, and process-driven storage in `src/storage/host.ts` is host-only.

## WASM Isolation Policy (COOP/COEP)

`Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Embedder-Policy: require-corp` opt the page into cross-origin isolation, which gates access to `SharedArrayBuffer` and multi-threaded WASM used by the browser ZKP/theorem-proving stack (`src/services/zkp`). These headers cannot be set via a `<meta>` tag; they must be real HTTP response headers from the dev server, the preview server, and the production host.

- Vite dev/preview headers configured: Cross-Origin-Opener-Policy, Cross-Origin-Embedder-Policy
- Deployment `_headers` file: `web/public/_headers` (exists: yes)
- Deployment headers configured: Cross-Origin-Opener-Policy, Cross-Origin-Embedder-Policy

### Hosting equivalents

Static hosts that do not read a `_headers` file (Netlify/Cloudflare Pages format) must reproduce the same headers at the server/CDN layer:

- **Vercel**: add a `headers` array to `vercel.json` with the same header/value pairs as `web/public/_headers`.
- **nginx**: `add_header Cross-Origin-Opener-Policy same-origin always;` and `add_header Cross-Origin-Embedder-Policy require-corp always;` in the `server`/`location` block serving `dist/`.
- **Apache**: `Header always set Cross-Origin-Opener-Policy "same-origin"` and `Header always set Cross-Origin-Embedder-Policy "require-corp"` in `.htaccess` or the vhost config.
- **Cloudflare Workers / custom Express static servers**: set the same headers explicitly on every response, including cached ones.

WASM asset integrity (byte length + SHA-256 manifest metadata) is tracked separately by the browser ZKP artifact registry; see `docs/browser-zkp-artifacts.md`.

## Offline Mode Policy

| Artifact | Path | Present |
| --- | --- | --- |
| Service worker | `web/public/service-worker.js` | yes |
| Web app manifest | `web/public/manifest.webmanifest` | yes |
| Offline fallback page | `web/public/offline.html` | yes |

- Service worker `install`/`fetch`/`activate` handlers present: yes
- Service worker uses Cache Storage API: yes
- Service worker host-only imports found: 0

Strategy (see `web/public/service-worker.js` for the implementation):

- Navigation requests: network-first, falling back to the cached app shell and finally to `/offline.html`.
- Same-origin static asset requests (script/style/image/font/worker): stale-while-revalidate against a runtime Cache Storage bucket.
- Everything else (cross-origin requests, non-GET requests, IPFS gateway traffic): left entirely to the network so the service worker never intercepts non-idempotent requests or MCP/IPFS traffic with its own retry policy.
- A minimal, author-maintained app shell precache list is used instead of a hashed-filename precache manifest, since this deployment has no separate PWA build step.

## Bundle Host-Leakage Check

Metadata: `dist/.vite/swissknife-bundle-metadata.json` (present: yes)

No host-only worker or storage modules found in the built browser bundle.

## Findings

No browser deployment policy findings.
